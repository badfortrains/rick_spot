# Do training
gcloud compute instances create rick-training-spot-1 \
    --project=$MY_PROJECT_ID \
    --zone=us-west1-a \
    --machine-type=g2-standard-12 \
    --provisioning-model=SPOT \
    --maintenance-policy=TERMINATE \
    --image-family=common-cu129-ubuntu-2204-nvidia-580 \
    --image-project=deeplearning-platform-release \
    --scopes=https://www.googleapis.com/auth/cloud-platform \
    --metadata=install-nvidia-driver=True,target_bucket=$MY_BUCKET_NAME \
    --metadata-from-file startup-script=./startup.sh
    
# Setup the enviornment, used to build a durable image
gcloud compute instances create rick-training-spot-1 \
    --project=$MY_PROJECT_ID \
    --zone=asia-east1-a \
    --machine-type=g2-standard-12 \
    --provisioning-model=SPOT \
    --maintenance-policy=TERMINATE \
    --image-family=common-cu129-ubuntu-2204-nvidia-580 \
    --image-project=deeplearning-platform-release \
    --scopes=https://www.googleapis.com/auth/cloud-platform \
    --metadata=install-nvidia-driver=True,target_bucket=$MY_BUCKET_NAME \
    --metadata-from-file startup-script=./setup_env.sh

# Run training using the custom image
gcloud compute instances create rick-training-spot-1 \
    --project=$MY_PROJECT_ID \
    --zone=us-west1-a \
    --machine-type=g2-standard-12 \
    --provisioning-model=SPOT \
    --maintenance-policy=TERMINATE \
    --source-machine-image=rick-spot-base-image \
    --scopes=https://www.googleapis.com/auth/cloud-platform \
    --metadata=install-nvidia-driver=True,target_bucket=$MY_BUCKET_NAME

    source /opt/venv/bin/activate
    cd /opt/rick_spot
    uv pip install -r requirements.txt
    train-jax-ppo --env_name RickJoystickSimpleFlatTerrain

export MUJOCO_GL="egl"
    train-jax-ppo \
  --env_name RickJoystickSimpleFlatTerrain \
  --play_only \
  --load_checkpoint_path /opt/rick_spot/logs/RickJoystickSimpleFlatTerrain-20260816-190657/checkpoints

    curl -H "Metadata-Flavor: Google" http://metadata.google.internal/computeMetadata/v1/instance/attributes/target_bucket; echo;
    gcloud storage cp /home/s3purcell/logs/RickJoystickSimpleFlatTerrain-20260814-184407/rollout0.mp4 gs://$GCS_BUCKET_NAME/rollout2.mp4


